package com.mssus.app.common.enums;

public enum ReportPriority {
    LOW,       // Low priority - can be handled in due course
    MEDIUM,    // Medium priority - default level
    HIGH,      // High priority - needs prompt attention
    CRITICAL   // Critical - requires immediate attention
}

