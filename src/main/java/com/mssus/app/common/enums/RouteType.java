package com.mssus.app.common.enums;

/**
 * Describes how a route definition was created.
 *
 * TEMPLATE: curated by admins for recurring corridors.
 * CUSTOM:   snapshot captured for a specific ride instance.
 */
public enum RouteType {
    TEMPLATE,
    CUSTOM
}
